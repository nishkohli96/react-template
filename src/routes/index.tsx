import { BrowserRouter } from 'react-router';
import { AppBar } from 'components';

const Routing = () => {
  return (
    <BrowserRouter>
      <AppBar />
    </BrowserRouter>
  );
};

export default Routing;
